create database if not exists sql_mastery_day2;
use sql_mastery_day2;
create table data_types_demo(
id int,
salary decimal(10,2),
mark float,
name varchar(50),
description text,
created_at date,
Login_time timestamp
);